# NETGuard
A Python-powered security tool designed to identify and assess devices on a local area network (LAN). Unlike standard discovery tools, NetGuard performs stealthy TCP SYN scans to map open ports and identify potential entry points—such as unpatched SMB or exposed SSH ports—without alerting traditional logging systems.
